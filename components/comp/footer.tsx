import {
    Pagination,
    PaginationContent,
    PaginationEllipsis,
    PaginationItem,
    PaginationLink,
    PaginationNext,
    PaginationPrevious,
} from "@/components/ui/pagination"

export default function Footer() {
    return (
        <div className="flex justify-center content-center">
            <p className="text-gray-400">——— ✨ Wish u have a nice Trip too ✨ ——— </p>
        </div>
    )
}